# 间隙和振动解耦工作区

当前主工程：

- `GapVib_Ortho_Verify`

数据与旧基线：

- `data`

文档：

- `docs`

历史工程归档：

- `archive`

推荐先进入：

```matlab
cd GapVib_Ortho_Verify
run('00_current_two_vp_main/Step01_Run_Current_TwoVP_Key_Cases.m')
```
