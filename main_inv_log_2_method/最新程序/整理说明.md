# 最新程序

本文件夹保留当前 Route 30 低速标定与高速间隙—振动联合识别程序。

## 主入口

- `run_inv_log_2_low_high_main.m`：推荐的低速+高速完整入口。
- `run_inv_log_2_main_method.m`：当前统一主方法入口。
- `run_inv_log_2_structured_main.m`：显式频率结构入口。
- `main_single_case_manual/Run_Main_Single_Case.m`：单工况手动演示。

## 目录

- `local_func`：当前主方法依赖函数。
- `30_low_high_gap_fast_bridge`：Route 30 验证程序、说明和历史运行输出。
- `theory`：与当前实现对应的理论审核材料。
- `trust_domain_result`：当前固定可信空间结果。

`30_low_high_gap_fast_bridge/output` 为已有的实验结果，本次整理未删除、未覆盖。
