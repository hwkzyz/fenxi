# 旧仿真程序

本目录保存 2026-09-05 整理前的历史仿真脚本，仅用于追溯和对照，不属于当前主程序。

当前主程序在上一级目录，按以下顺序运行：

`Main_01_BuildSimulationSurface` -> `Main_02_PC1SurfaceValidation` ->
`Main_03_OpenSetTransfer` -> `Main_04_BoundaryIdentifiability` ->
`Main_05_PC1Figures`

这里的 `Analyze_*`、`Plot_*`、`Run_*` 和 `Summarize_*` 文件不应作为最新结果入口。
