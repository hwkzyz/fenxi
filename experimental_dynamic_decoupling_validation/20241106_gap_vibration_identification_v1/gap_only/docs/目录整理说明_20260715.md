# 目录整理说明

本次整理只改变正式程序的可见顺序、文件位置、结果时间标签和必要路径，不改变Foundation或GapAware的辨识模型。

- 原根目录四个 `Run_*` 包装入口及原 `src` 完整快照保存在 `archive/first_package_wrappers_20260715`；
- Foundation、DynamicMap、GapAware和正式比较代码已作为真实程序放到根目录；
- 应变共振、时间对齐、频率/幅值验证和最终波形可视化纳入 `Main02`、`Main11`—`Main13`；
- OPR及窗口选取的交互查看程序放入 `diagnostics/data_quality`；
- 结果文件加入分析时间标签，避免不同时间点互相覆盖；
- 正式配置中不存在参考阶次，且 `eta=0`、最终普通电压RMSE约束保持不变。

旧目录 `20241106_low_speed_gap_prior_decoupling` 未作修改。
