# Pre-unification formal program snapshot

These files are the formal 20251222 entry, configuration, and package check
immediately before the 2026-08-11 simulation/experiment theory unification.

The snapshot used the fixed low-speed median/mean template and described the
candidate layer as a frozen VP-only route. The active root program now uses
the common structured-voltage theory contract and an adaptive-SG + PCHIP
low-speed template while retaining the same final complete voltage model.
